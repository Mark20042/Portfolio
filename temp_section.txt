<!-- Featured Projects -->
	<div class="flex flex-col gap-6 sm:gap-8 mb-12">
		{#each featuredProjects as project}
			<button
				type="button"
				onclick={() => selectedProject = project}
				class="relative w-full rounded-3xl overflow-hidden group shadow-lg hover:shadow-2xl hover:-translate-y-1 transition-all duration-500 bg-slate-50 dark:bg-[#111] border border-slate-200 dark:border-[#222] text-left cursor-pointer"
			>
				<div class="flex flex-col md:flex-row">
					<div class="flex-1 p-6 sm:p-8 flex flex-col justify-center">
						<p class="text-xs font-bold text-sky-500 uppercase tracking-wider mb-2">{project.category}</p>
						<h3 class="text-xl sm:text-2xl font-bold text-slate-900 dark:text-white mb-3">{project.title}</h3>
						<p class="text-sm text-slate-600 dark:text-slate-400 leading-relaxed line-clamp-3">{project.description}</p>
						<div class="flex flex-wrap gap-2 mt-4">
							{#each project.techStack as tag}
								<span class="cursor-default rounded-md border border-slate-300 px-2 py-0.5 text-[11px] font-medium text-slate-700 dark:border-[#3a3a3a] dark:text-slate-300">
									{tag}
								</span>
							{/each}
						</div>
					</div>

					<div class="relative w-full md:w-[45%] h-48 sm:h-64 md:h-auto min-h-[200px] bg-slate-200 dark:bg-[#1a1a1a] overflow-hidden">
						{#if project.images && project.images.length > 0}
							<div class="flex h-full overflow-x-auto snap-x snap-mandatory scrollbar-hide">
								{#each project.images as img}
									<img
										src={img}
										alt={project.title}
										width="800" height="600" loading="lazy" decoding="async"
										class="h-full w-full object-cover shrink-0 snap-center"
									/>
								{/each}
							</div>
						{:else}
							<img
								src={project.image}
								alt={project.title}
								width="800" height="600" loading="lazy" decoding="async"
								class="h-full w-full object-cover"
							/>
						{/if}
					</div>
				</div>
			</button>
		{/each}
	</div>

	<!-- Modal -->
	{#if selectedProject}
		<!-- svelte-ignore a11y_click_events_have_key_events -->
		<!-- svelte-ignore a11y_no_static_element_interact
